services:
  db:
    image: postgres:18
    environment:
      POSTGRES_DB: creators
      POSTGRES_USER: creators
      POSTGRES_PASSWORD: creators
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
